// Clase Nodo
class Nodo {
    constructor(letra) {
      this.letra = letra;  // Almacena una letra
      this.siguiente = null;   // Apunta al siguiente nodo
      this.anterior = null;    // Apunta al nodo anterior (doblemente enlazada)
    }
  }
  